package com.example;

// simple demo app with CRUD functions using Hibernate
public class App {
  private SessionFactoryProvider sessionFactory;

  public App(SessionFactoryProvider sessionFactory) {
    this.sessionFactory = sessionFactory;
  }

  

}
